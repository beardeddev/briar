@model IEnumerable<Briar.Models.Tag>
           
@Display.TaggedUnder:
<ul>
@foreach (var tag in Model)
{
    <li>
    @Html.ActionLink(tag.Title, "Details", "Tags", new { @url = tag.Url }, new { @title = tag.Title })
    </li>
}
</ul>
